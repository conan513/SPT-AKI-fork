{
  "name": "sptarkov-server",
  "version": "1.0.0",
  "main": "scripts/main.js",
  "author": "SPTarkov",
  "license": "MIT",
  "scripts": {
    "build": "npm run version-check && node dev/src/build.js",
    "start": "npm run version-check && node core/main.js",
    "lint": "eslint --fix ./**/*.js",
    "version-check": "node core/util/check-version.js"
  },
  "dependencies": {
    "nexe": "^3.3.7",
    "selfsigned": "^1.10.7",
    "upx": "^1.0.6"
  },
  "devDependencies": {
    "eslint": "^7.8.1"
  }
}
